---
title: AIPET 2026-05 session 归档
updated: 2026-05-11
related:
  - ../../STATUS.md
  - ../../lessons.md
---

# 2026-05 session 归档

> 从 STATUS.md 拆分而来，覆盖立项准备期（2026-04-30 → 2026-05-05）+ M1 W1-W2 主体（D1-D15，2026-05-06 → 2026-05-11；17/18 issue 完成，剩 [#21](https://github.com/tl0502/APET/issues/21) M1 收尾）。
>
> 当前进度看 [../../STATUS.md](../../STATUS.md)；踩坑总结看 [../../lessons.md](../../lessons.md)；任务详情看 [GitHub Issues](https://github.com/tl0502/APET/issues)。

---

## 已完成（里程碑级摘要）

### M1 启动（2026-05-06 起）

- M1-D1 项目脚手架就位（commit 8952e6e）：Tauri 2 + Vue 3 + TS + Pinia + Vite 7 + pnpm；`pnpm tauri:dev` 跑通 320×320 透明窗口；ADR-016 / ADR-017 入库（commit 3426184）。详见关闭的 [Issue #1](https://github.com/tl0502/APET/issues/1)。
- M1-D2 Element Plus + 主题跟随系统就位（commit 7c387db）：EP 全量 import + zh-CN locale + 三态 Pinia 主题 store（auto/light/dark）+ matchMedia/localStorage；`pnpm tauri:build` 实测 release exe = **4.15 MB**（vs 预估 ~9MB，偏好 53%），ADR-017 已补实测；同 commit 清理 4 处文档过时性能预算（`启动 < 1500ms / 内存 < 150MB` → 推到 M5 自测期统一压测）。详见关闭的 [Issue #2](https://github.com/tl0502/APET/issues/2)。
- M1-D3 PetCanvas + VRM momo 渲染就位（commit 0df9076）：three@0.184 + @pixiv/three-vrm@3.5；vrm.ts（VRMRuntime 透明背景 + 半身相机 + 1.6Hz 呼吸 + A-pose + spring bone）+ useVRMModel composable + 简化版 PetCanvas（193L → 71L，剥离 hitbox/drag/IPC，推到后续 task）；avatar.vrm 用户私有 .gitignore 屏蔽。详见关闭的 [Issue #3](https://github.com/tl0502/APET/issues/3)。
- M1-D4 IPC 框架就位（commit ff32dda）：Rust `commands::system::ping` + `lib.rs` 注册 `invoke_handler![ping]`；前端 `services/ipc.ts` 统一 `invoke<T>` wrapper + `types/ipc.ts` `IpcError`（带命令名上下文）；M1 不引 ts-rs/specta，类型手写。详见关闭的 [Issue #4](https://github.com/tl0502/APET/issues/4)。
- M1-D5 PersonaService MVP + Memory/Nickname 骨架就位（commit e5ca882）：tauri-plugin-sql 2 + sqlx 0.8 + gray_matter 0.2 + ulid 1；001_init.sql 全 27 表一次建（M2-M5 零迁移）+ 002 persona_snapshots unique idx；启动期 spawn `seed_builtin` UPSERT 内置 momo（personas + persona_snapshots）；11 个 IPC 命令（persona_load/activate + nickname 5 个 + memory KV 4 个）。**整文件复用**旧项目 D:\Project\ai桌宠 dogfood 过的 4 个 .rs + 2 个 SQL + momo.soul.md（38 个测试自带）。详见关闭的 [Issue #5](https://github.com/tl0502/APET/issues/5)。
- M1-D6 系统托盘 + 关闭语义就位（commit 558dafb）：tauri features 加 `tray-icon`；新建 services/{tray,window_actions}.rs（**整文件复用 + 裁剪**旧项目 D:\Project\ai桌宠 cut/window 的 dogfood helper：tray.rs 125→105L 删 PASSTHROUGH/TOPMOST 2 个 CheckMenuItem，window_actions.rs 35L 0 改）；lib.rs 接入 tray::setup + on_window_event 拦截 CloseRequested → window.hide()，唯一退出路径 = 托盘"退出"。**关键取舍**：「显示/隐藏」1 项动态文案 + 「单击托盘 toggle」按用户决策删除（仅菜单可操作避免误触）；decorations:false 故无 X 按钮，关闭只走 Alt+F4。详见关闭的 [Issue #6](https://github.com/tl0502/APET/issues/6)。
- M1-D7 视觉 token 体系 + 主题完整化就位：新建 `src/styles/tokens.css`（light/dark 双套 `--aipet-*`，覆盖 color / spacing / font / radius / shadow / motion / z-index 7 组）+ `src/styles/element-overrides.css`（用项目 token 覆写 `--el-*` 关键变量）+ `src/composables/useTokens.ts`（JS 侧 `getToken` / `getAipetToken`）+ `src/views/_dev/TokensPreview.vue`（dev-only `?view=tokens` 视觉对照页）；`src/main.ts` 调整 CSS 加载顺序（EP 默认 → dark css-vars → tokens → EP 覆写 → main.css）+ dev-only 动态 import preview 组件；`src/components/PetCanvas.vue` 提示样式作为首个 token 消费示例。**同 session 顺手修复 plugin-sql migrations 时序**（独立 commit）：`tauri.conf.json` 加 `plugins.sql.preload=['sqlite:aipet.db']`，让 plugin setup 阶段同步 connect + migrate，使 builder.setup 里 `block_on(seed_builtin)` 不再撞 SQLITE_CANTOPEN(14)（D5 完成时是 spawn 异步、前端先调过 plugin load 建库；2026-05-06 code-review 把 spawn 改 block_on 反转了顺序，引入此回归）。详见关闭的 [Issue #7](https://github.com/tl0502/APET/issues/7)。
- M1-D8 通用布局容器就位（commit 82ec827）：新建 `src/components/layouts/AppShell.vue`（两 variant：standalone 含 `[data-tauri-drag-region]` header + body + 可选 footer slot；transparent 纯语义包装无 chrome）+ `src/components/feedback/StandardDialog.vue`（包 EP ElDialog，X / 遮罩 / ESC 三关闭路径透传，loading=true 屏蔽 + spinner overlay + footer 灰禁）+ `src/composables/useToast.ts`（包 EP ElMessage，4 方法 success/error/info/warn=EP 'warning' + ToastAction 闭包绑 instance.close()）+ `src/styles/components.css`（.aipet-shell / .aipet-dialog / .aipet-toast 样式覆写，全部 `--aipet-*` token）；`src/main.ts` CSS 加载序加 components.css（EP → dark → tokens → overrides → components → main）；`src/App.vue` 用 `<AppShell variant=transparent>` 包装 PetCanvas 真实生产消费；`src/views/_dev/TokensPreview.vue` 追加 Components section（4 toast 按钮 + open dialog + toggle loading）+ 修 preview 可滚动（`min-height: 100vh` → `height: 100vh; overflow-y: auto`，因 main.css 给 html/body/#app 设了 overflow:hidden 适配 pet 透明窗）。**关键取舍**：① 拖动用 Tauri 2 推荐的 `[data-tauri-drag-region]` 而非 issue body 字面 `-webkit-app-region: drag`（后者仅 macOS，Windows WebView2 不识别）；② AppShell 不重 init theme（main.ts 全局已调）；③ StandardDialog 不内置 cancel/confirm（footer slot 自由）；④ Toast 按钮 issue 字面 3 个 → 实际 4 个完整覆盖 4 方法验收。详见关闭的 [Issue #8](https://github.com/tl0502/APET/issues/8)。
- M1-D9（2026-05-07）三 issue 同日完成 = M1 W2 主态可达：① 设置面板独立窗口骨架（commit 44bafbf）：multi-page Vite + settings.html + Tauri 静态注册 settings 窗（800×600 / decorations:true / visible:false / 关闭=hide 不销毁）+ 5 tab 骨架（主题完整 / Provider 6 preset 静态 / 人格 loadPersona 仅展示 / 昵称 nickname IPC 仅展示 / 关于 getVersion）+ 托盘"设置"激活 + theme storage event 跨窗口同步（同 origin webview 共享 localStorage 零开销）+ components.css `.aipet-shell--standalone` 补 `height:100%` 让 ElTabs `tab-position=left` 高度链路完整（详见关闭的 [Issue #9](https://github.com/tl0502/APET/issues/9)）。② 桌宠拖动 + 多屏位置持久化（commit 9d6c7e9 + fix 2a34cf7）：PetCanvas pointerdown → `startDragging` + Rust `WindowEvent::Moved` 200ms 防抖（SaveDebouncer）+ services/window_state.rs（compute/set/load/clamp/fallback/apply_initial_position）+ 启动期 monitor 匹配 + 16px 安全边距裁剪 + fallback 主屏右下角偏左 80px。**关键偏离**：schema 不保留 `pet_runtime_state.last_position` 字段（"27 表零迁移" D5 原则），改用现有 `config` 表 KV（"运行时配置走此表"，与 `active_conversation_id` 同表，语义零损失）。**关键 fix**：Tauri 2 capability 严格 grant，`core:default` **不含** `core:window:allow-start-dragging` / `set-position` / monitor 系列，必须显式 allow 到 `capabilities/default.json`，否则 startDragging 静默无反应——第一次 dev 视觉验证暴露此问题（详见关闭的 [Issue #10](https://github.com/tl0502/APET/issues/10)）。③ 全局快捷键 Ctrl+Alt+Space + 冲突探测（commit fd227a2 + fix ff318b9）：tauri-plugin-global-shortcut@2 + capability allow + services/shortcuts.rs（register/probe/set_chat_shortcut；handler emit `shortcut:chat`）+ commands/shortcuts.rs IPC + App.vue listen → toast.info stub（#14 ChatPanel ready 后改 `invoke('chat_show')`，注释已埋）+ shortcut 持久化走 `config` 表 KV（同 #10 偏离 schema 没 settings 表）。**关键 fix**：plugin 全局 `with_handler` 在 Windows 下不触发（global_hotkey set_event_handler ↔ plugin handler 分发链路在某些 OS message pump 路径上失效），改用 `on_shortcut(s, handler)` 把 handler 直接绑到 `RegisteredShortcut.handler` 字段；启用 `tauri.conf.json app.withGlobalTauri:true` 让 DevTools console 能调 IPC（详见关闭的 [Issue #11](https://github.com/tl0502/APET/issues/11)）。
- M1-D10（2026-05-07，#9-#11 同日）LLMProvider OpenAI MVP + ADR-018 三层抽象框架（4 commit + 1 fix + 1 CUSTOM 扩展 + 1 STATUS 同步）：**ADR-018 入库**（commit 02fe910）锁三层职责 — Layer 1 LLMProvider（消息进 token 出 + tool_call 透传，#12 落地）/ Layer 2 ChatService（编排 Persona+Memory+SecurityGuard，#13 起完整）/ Layer 3 AgentService + ToolRegistry（Claude Code 风格 agent loop + Read/Edit/Write/Glob/Grep/Bash 6 工具，M3+ 新增；沙盒细则推 ADR-019），同时覆盖架构 §6.1（callback 取代 impl Stream + Vec<ContentPart> 取代 String content；架构原文已加 superseded 注）。**LLMProvider trait + OpenAIProvider 文本流式**（commit fddbe5e）：types 用 `Vec<ContentPart>` parts 数组（Text/ImageUrl/ImageBase64/Audio/File 5 variant）+ ToolCall/ToolDefinition typed only（M1 实际只走 Text + tools=vec![]，M3+ 接多模态/工具调用不动 trait）；OpenAIProvider 流式 + 取消（reqwest bytes_stream → eventsource_stream → on_delta callback；cancel.cancelled() 触发立即 break + drop stream 自动断 TCP）；error 7 分类（Network / AuthFailed / RateLimit / BadRequest / ServerError / Cancelled / ParseError）；序列化策略：单 Text part 序列化成旧 string `{role,content:"s"}` 兼容老 model 与 Ollama/Qwen/Moonshot fallback，多 part 走 parts 数组；process_event_stream 抽出来用合成 Bytes 流单测无需 mock HTTP server。**LLM IPC + 前端 binding**（commit c3a0f85）：4 个 IPC（set_openai_api_key / get_openai_api_key_set / chat_send_test / cancel_test）+ ActiveTestRegistry Mutex<Option<CancellationToken>> 跨 IPC 共享；chat_send_test emit `chat:test:delta` 让 dev console listen 验真流式。**CUSTOM provider 扩展**（commit 11dc2cd）：加 set_openai_config({api_key?, base_url?, model?}) + get_openai_config() 让 dev console 用任意 OpenAI 兼容端点（DeepSeek/Moonshot/Qwen/Ollama）验证。**关键 fix tokio macros feature**（commit a93bf0d）：cargo feature 统一陷阱 — dev-deps tokio 启了 macros 让 cargo test 能跑，但非 test build（`pnpm tauri:dev`）不继承 dev-deps features，撞 'cannot find select in tokio'；补 `macros` feature 到主 deps；**M1 W2 起 cargo check 必须额外跑一遍 lib-only 路径**（不带 `--tests`）才算真过。**真实 dev 验证**（用户 DeepSeek key）：① 黄金路径（中文回复 + emit chat:test:delta 流式）✅；② cancel 中途取消 → `Cancelled: cancelled by caller` ✅；③ AuthFailed 长 fake key 触发 DeepSeek 401 → `AuthFailed: 401 ...` ✅（**provider quirk**：短 sk-WRONG 在 HTTP/2 层被 DeepSeek RST，落 Network 兜底；长 fake key 通过格式校验进入 status code 路径才能拿 401；OpenAI 在国内 GFW 拦 TLS 也只能拿 Network；classify_status 5 分支单测已证代码正确）；④ Network 假 host 触发 ✅；⑤ cargo test 63 passed（原 46 + 新 17：types 5 + error 5 + openai 7）。**关键偏离**：schema 不保留 issue body 字面"settings 表 openai_api_key 字段"（#10 #11 同款守 27 表零迁移 D5 原则），改用 `config` 表 KV（key=`llm:openai:{api_key,base_url,model}`，M3 G CryptoService 上线后 api_key 迁 secrets+DPAPI）；issue body 字面只允许 `set_openai_api_key` 一个写入点 → 加 `set_openai_config` 三键 partial update 让 CUSTOM provider 落地（架构 §0.1 6 preset 含 custom）。详见关闭的 [Issue #12](https://github.com/tl0502/APET/issues/12)。
- M1-D11（2026-05-07，#12 后续）ChatService MVP + ADR-018 系统 prompt 拼装就位（2 commit：fmt cleanup b6a8926 + ChatService 主体）：M1 W2 B 模块"真业务对话"上线。**新增 4 个 .rs + 2 个 .ts 共 6 个文件**：services/chat/{prompt, conversation, service, mod}.rs（service 编排 send/cancel/history 三方法 + active_streams cancel token map + 4 分支收尾：成功/取消/网络降级/其他错误；prompt 切人格 4 节 + 拼 system message + wrap_user_input 反 drift 包装；conversation KV 复用/孤儿自愈）+ commands/chat.rs（3 IPC：chat_send 返 {messageId, conversationId}、chat_cancel、chat_history）+ src/types/chat.ts（Message + 3 stream payload + ChatFinishReason）+ src/services/chat.ts（sendChat/cancelChat/loadChatHistory + 3 个 onChat* listen wrapper）。**ADR-018 落地三层都有真实 caller**：Layer 1 LLMProvider trait 被 ChatService 通过 OpenAIProvider 现建模式消费（不 hardcode 具体 provider，M3 多 provider 改 ProviderRegistry 一行即可）；Layer 2 ChatService 编排 Persona+Nickname+Memory+Conversation+LLMProvider 5 service；Layer 3 AgentService 推 M3+。**关键设计（基于 claw-code 源码 + 学术研究）**：① **切区段不全塞**（`# 离线模板` / `# 反应配置` 是本地代码池/物理交互模块用，塞 LLM 是噪音）— 切 `# 身份/# 性格/# 能力/# 行为规则` 4 节走 LLM；遇到 `# 离线模板`/`# 反应配置` 立即终止；其他 H1（`# 例对话` / `# 集成`）退出当前累积继续扫描；section 上限 4000 chars 防恶意人格爆 token；② **inline re-anchor 反 drift**（[arxiv 2402.10962](https://arxiv.org/html/2402.10962v1) 学术支持的 cheap technique）— system message 末句 "保持上述身份与性格设定。回复偏离 {persona_name} 的语气时，立即回到原风格。" + wrap_user_input 把每条 user 包装成 "（保持 {persona_name} 风格）{raw_input}"；DB 仍存原始 input（ChatPanel 显示原文），仅 LLM 调用前包装；③ **昵称结构化 bullets**（不混在自然语言里）+ pet_nickname == persona_name 时跳过 bullet 减冗余；④ **静态/动态 boundary 设计预留**（M3 prompt cache breakpoint，M1 不实现 sentinel 但函数拆分到位）；⑤ **离线降级抽 # 拒答** 池（不是 issue body 字面的 # 共情，"网络断了"语义最贴 # 拒答）+ 仅 LLMError::Network/ServerError 触发（AuthFailed/BadRequest 不抽，掩盖配置错误反害用户）；⑥ **Provider 每次现建** 与 #12 chat_send_test 同模式（用户改 config 立即生效，无 hot reload 烦恼）；⑦ **partial 不入库**（schema messages 表无 status 列，27 表零迁移；只 emit chat:stream:error 让用户重发）。**关键偏离 issue body**：① schema 没 user_state 表（D5 原则），active_conversation_id 走 config KV；② SendResult 加 conversationId 字段（issue body 仅 messageId，dev 验收/#14 ChatPanel 切会话用得上，省一次 IPC）；③ load_active_persona 新增 helper（PersonaService 原本只有 load_persona(id)，ChatService 用 None 时需自动取 active）。**真实 dev 验证**（用户 DeepSeek 真 key + 拔网卡）：① 黄金路径 chat:stream:delta 流式 + done finishReason=stop ✅；② 二次 send 不传 conversationId 复用 ✅；③ 取消中途 → done finishReason=cancelled + partial 入库 ✅；④ chat_history 升序返 ✅；⑤ 离线降级抽 # 拒答 + emit delta 一次 + done finishReason=offline_rule + mode='offline_rule' ✅；⑥ 错 API key → chat:stream:error errorKind='AuthFailed' ✅；⑦ cancel unknown id no-op ✅。**测试**：cargo test --lib 96 passed（原 63 + 新 33：prompt 14 + conversation 5 + service 11 + 其他 3）；clippy chat/* 0 新 warning；pnpm typecheck 0 错；cargo fmt 全仓库对齐 11 个 #5/#12 历史文件作单独 cleanup commit（b6a8926）。**ADR-018 关键学习**：Anthropic AUP 触发坑 — 上次 session 中途因生成代码时疑似涉及 ADR-006 安全前缀完整文案 / 泄露 API key / 离线模板大段渲染被过滤器命中；本次以"安全前缀严守 M1 占位"+"API key 全程占位 sk-..."+"离线模板抽样而非渲染"+"每文件分开写"4 条规则恢复后顺利完成。**用户产出 system prompt 管理指南** 23 KB / 593 行落盘 `D:\Project\temp\记录\ai桌宠-system-prompt-管理指南.md`（覆盖整体架构 + 9 个微调场景 + 3 级验证 + 5 个陷阱 + 版本管理 + 代码地标表）。详见关闭的 [Issue #13](https://github.com/tl0502/APET/issues/13)。

- M1-D12（2026-05-08，#13 修正）IPC 流式契约从全局 emit 迁移到 `tauri::ipc::Channel<StreamEvent>`：M1-D11 #13 的 `chat:stream:{delta,done,error}` 三个全局 `app.emit` 事件契约在 #14 ChatPanel 用上后暴露根因 bug — `chat_send` 是 `async fn` 直到流式跑完才 resolve，前端拿不到真实 `assistant_id` 全程，**导致取消按钮整段流式期间是死按钮**（temp-id 守卫 `startsWith('streaming-')` 直接 return），叠加切换会话在 `isStreaming=true` 时被禁，**用户被锁死直到流自然结束**。**新契约**：`chat_send(input, conversationId?, onStream: Channel<StreamEvent>)` —— 内部拆 `prepare`（同步：写 user msg / 注册 cancel token / 拼 prompt / build provider）+ `run_stream`（spawn 异步：流式 + 4 分支收尾），IPC 立即返 `SendResult { messageId, conversationId }` 给前端 → cancel 立即可用；`StreamEvent` 是 typed enum（`Delta` / `Done` / `Error` 三 variant，`#[serde(rename_all = "camelCase", tag = "type")]` + 各 variant 内 fields 单独标 `rename_all` —— 顶层 `rename_all` 不会传递到 variant 内字段，第一遍跑测试踩到这个序列化坑）；channel 自带 scope 故 payload 不再带 messageId（每条 channel 只服务一个 assistant 消息）。**前端简化**：删 `inflightTempId` / `inflightDoneInfo` / `inflightErrorInfo` 三 ref + 整套缓冲/drain 逻辑（约 60 行）；删 onMounted 里 3 个全局 `listen()` 注册；`handleCancel` 删 `startsWith('streaming-')` 死路；`handleSend` 改用 `new Channel<StreamEvent>()` + `channel.onmessage` switch 三 variant + `inflight` 对象兜底（早期 token 缓冲，spawn 在 prepare 后 schedule 故理论上不会触发，但 TS 5 strict 模式下闭包内 `let` 赋值不会跨闭包 narrow，必须用对象包裹才能 if 后正确窄化为非 null）。**未迁移**：`llm:test:delta`（用户增补 LLM Providers 的事件，独立 issue 处理）；`nickname:changed` / `persona:activated` / `shortcut:chat`（真广播事件，多窗口都要听，Channel 不适合，保留 emit）。**关键文件**：service.rs（拆 send → prepare + run_stream + 加 StreamEvent enum + 删 3 个事件常量 + 3 payload struct + 3 helper + ChatService 加 `Clone` 让 `service.inner().clone()` 进 spawn）/ commands/chat.rs（chat_send 加 `onStream: Channel<StreamEvent>` 参数 + 内部 `tauri::async_runtime::spawn`）/ types/chat.ts（删 3 个 payload interface + 加 `StreamEvent` discriminated union）/ services/chat.ts（删 3 个 EVENT 常量 + 3 个 listener wrapper + sendChat 加 `onStream` 参数 + re-export `Channel`）/ ChatApp.vue（如上简化）。**测试**：cargo test --lib **121 passed**（原 117 + 新 4：3 个 StreamEvent 序列化测试 + service_clone_shares_active_streams）；pnpm typecheck / lint --max-warnings=0 / build 三件套 0 错（build 9.13s）。**dev 集成验证**（用户）：黄金路径 / 取消（核心修复，cancel 立即生效）/ 取消后切换会话 / 网络降级 / AuthFailed / 并发隔离 6 项全过。**关键学习**：Tauri 2 中长流式 IPC 必须用 `tauri::ipc::Channel`，不要用 `app.emit` —— 单 IPC 中无法在 stream 完成前返 id，导致 cancel 路径死锁；详 lessons.md #6。详见 [Issue #13](https://github.com/tl0502/APET/issues/13)（修正 comment）+ commit `6b6b7bb`。

- M1-D13（2026-05-09 → 2026-05-10，#14 ChatPanel V3 多对话并发版，commit a9f4b2c）：原 issue body 计划"380×480 单 conversation 极简版"，开发期发现与 ChatGPT 式侧边栏基线认知差距过大，**将 M3 B.3.d 多会话 CRUD UI 下沉到本 issue 一并落地**。**新增 9 个 IPC**：多会话 CRUD（`chat_list_conversations` / `create` / `set_active` / `rename` / `archive` / `delete`）+ 草稿 KV（`chat_get_draft` / `set_draft` / `delete_draft`，键 `chat:draft:<convId>`）+ 窗口（`chat_show` / `hide` / `toggle`，新建 commands/window.rs）。**V3 关键决策（10 条）**：① **每对话独立 ConvState（reactive Map）** — ChatGPT 风格基线认知；后端早已并发就绪，前端单 `messages` ref 把它锁死，拆桶后自然解锁；② **Channel 闭包路由** — `onmessage` 闭包内捕获 `routedConvId / routedAssistantId`，事件直写对应桶，切走不丢 token；③ **首启 0 conv 防御** — `PENDING_CONV_KEY` 早期事件落 inflight buffer，sendChat resolve 后迁移到 realConvId；④ **switchSeq race 守护** — 用户快速点 A→B 时 Promise.all 顺序不可控，过期请求吞掉；⑤ **draft 双重 race 守护** — 切换 / 关窗前 `flushDraftIfPending` 强制 flush 未触发 timer + 首启 `pendingDraft` ref 兜底 await sendChat 期间持续输入；⑥ **启动期 GC 孤儿 placeholder** — 上次进程退出时 detached spawn 未收尾留下 content='' 行；cutoff 守护新进程的 placeholder 不被误删；⑦ **archived=0 三处守护** — ensure / set / update_last_activity（lessons §11）；⑧ **conversation 归属校验 + tx 内 INSERT** — caller 传 conv_id 时 SELECT persona_id 校验；FK 错误转译；BEGIN DEFERRED upgrade 死锁规避（SELECT 移出 tx，第一句 INSERT 直接拿写锁）；⑨ **mode='cancelled' + history filter 跳过** — 与 `offline_rule` 同理由（取消半句不是 LLM 真输出），空 partial DELETE；⑩ **ChatInput props 解耦** — `inputDisabled` / `sendDisabled` / `showCancel` / `cancelling` 四个独立，支持"流式中输入框可编辑、发送灰、切到空闲对话立即可发"的并发 UX。**关键偏离 body**：① 窗口 380×480 → **640×480**（极简过窄；sidebar 200 + 主区 280 难用）；② 首次定位"pet 右侧 +16px" → **center: true**（pet 320 + gap + chat 640 已超常见 1366 宽度；磁吸推 M2 B.3.c）；③ 单击桌宠唤起 **未做**（与 #10 拖动阈值未解 → M2 N）；④ M3 B.3.d 多会话 CRUD UI 本 issue 下沉（演化说明在 body）；⑤ "已取消"灰标 + 离线模板灰标共存（body 只写了后者）。**测试**：cargo test --lib **172 passed**（含 conversation CRUD / archived 守护 / orphan placeholder GC / chat commands validation 等新增）；typecheck / lint / cargo check / build 全过。**Follow-up**：M2 B.3.c 磁吸吸附 + pet 相对定位；M3 B.3.d 会话分组（今日 / 昨日 / 上周）+ 搜索 + 归档列表 UI + Markdown；M3 复制 / 重新生成 / 编辑 / 滚动定位。详见关闭的 [Issue #14](https://github.com/tl0502/APET/issues/14)。

- M1-D14（2026-05-10，#15 + #20 同日 2 commit）M1 W2 收尾两轮增补：
  - ① **#15 删 pet_nickname + 转场注入 announcement**（commit 0bd425f）：最终 IPC 4 个（`nickname_get_user` / `set_user` / `get_announce_user_change` / `set_announce_user_change`），删除 pet 系列 3 个 + 新增 announce 开关 2 个 = 净 -1（与 body 原数量相同但组合完全不同）。**关键决策**：a. **删除 pet_nickname 整条线** — 名字源唯一化为 `.soul.md persona.name`，避免双源；列冷藏不删（守 D5 零迁移）；b. **转场注入 system 行 vs 改 system prompt** — Persona Drift（[arxiv 2402.10962](https://arxiv.org/html/2402.10962v1)）证实长 history 下 system prompt 被稀释；history 中段 system 通知直接重置话术更稳定；c. **开关白名单 fail-safe** — 旧实现"仅 'false' 视 OFF"对隐私开关激进；改为仅 None / `"true"` → ON，用户手写错也 fail-safe；d. **archived=0 守护** — 跨窗口归档时 active KV 可能短暂指向归档行，不守会注入幽灵 system 行 + 刷归档行时间戳（与 chat/conversation.rs 三处守护理念对齐）；e. **非原子 trade-off** — `set_user_nickname_with_conn` 与 `maybe_inject_user_change` 不在同一 tx；中间崩溃留下"昵称已变但 conv 无转场"。M1 单机崩溃率低 + 注入辅助行为，可接受；M3+ 想严格化合并到一条 conn 顺序写；f. **payload 收窄不向后兼容** — `which: 'user'` 字面 + `value: string`；将来需要"清空"开专门 IPC，别复用 payload 重加 null。**关键偏离 body**：① 完全删除 U.1 桌宠昵称（body 原计划保留 pet/user 双列）；② IPC 数量 -3 / +2 = 净 -1；③ `nickname:changed` payload 收窄（删 pet / null 分支）；④ 新增 announcement 整条线（原 body 没设计过昵称变更对历史对话的影响）。**测试**：cargo test --lib 172 passed；nickname_announcement 8 个集成测试 + nickname 3 个 + validate_nickname 4 个单测全过。**Follow-up**：M2 H 人格工坊：切换人格时"昵称已重置 / 可恢复"提示（pet 名字源唯一化已是更清晰语义，建议保持当前 pet_nickname 删除状态）；M3 主体记忆完整版：称呼 / 作息 / 偏好字段统一在记忆模块管理。详见关闭的 [Issue #15](https://github.com/tl0502/APET/issues/15)。
  - ② **#20 LLM Providers 多实例管理 + 模型探测**（commit af07d90，M1 W2 增补）：6 preset 多 provider 管理 UI + 模型列表探测。**关键决策**：a. **schema 走 KV 而非新表** — 守 D5 27 表零迁移；每个 provider 一组 `llm:provider:<id>:*` 键（id / name / base_url / api_key / model / preset_type / created_at）+ `llm:active_provider_id` 单 KV 标 active；b. **启动期一次性迁移** — 检测 `llm:openai:api_key` 等 #12 老键存在 → 自动建 default provider + 设 active + 不删老键（向后兼容；M3 删旧路径前老键作为审计痕迹）；c. **探测走 OpenAI `/v1/models`** — 6 preset 协议都兼容；Ollama 本地探测无 api_key 时跳过认证头但仍走同一路径；d. **API Key 列表 UI 不回显完整值** — 仅显示首尾 4 字符 + 中间星号，避免 Drawer 编辑时误暴露屏幕共享场景；e. **删除 active 二次确认** — 不允许直接删 active provider（防止 ChatService 下一轮 send 找不到 provider 崩溃）；UI 提示先切换再删；f. **probe 走全局 emit `llm:test:delta`** — 测试事件是辅助路径短任务，多窗口订阅场景小，无需 Channel（lessons §6 反例集合再扩 1 项）。**关键偏离 body**：① "schema 落地时决定" → **走 KV 不新建表**（同 D5 守则下唯一可行选择）；② "set_active 给 ChatService 立即生效" → **Provider 每次现建模式**（#13 closing comment §4）天然支持，无需额外信号；③ #12 老键启动期自动迁移（body 拍板有提，落地确认做了）。**测试**：cargo test --lib 172 passed（含 llm_providers 测试：partial update / 迁移老键 / 设 active 等）；6 preset 手测连通 + 模型列表拉取。**Follow-up**：M3 G DPAPI 加密 API Key（届时一次性迁移已存 KV）；M3+ 自动重试 / 退避策略（按 errorKind 分级）/ Tool calls / Function calling / 非 OpenAI 协议（Anthropic / Gemini）。详见关闭的 [Issue #20](https://github.com/tl0502/APET/issues/20)。

- M1-D15（2026-05-11，#16 灵魂宣誓页，3 commit：主体 fdafeaa + UI polish 99f53f0 + capability fix e993bd6）：Onboarding Step 1（ADR-008 文案 v1.0）落地。**新增 10 个文件 + 改 5 个**：onboarding 独立 Tauri 窗 480×720（multi-page Vite + 静态注册，与 settings/chat 范式一致）+ SoulPledgeView 5 段宣誓动画 + soul_pledge_v1.txt 文案 + data_policy_v1.md 数据政策 + consent.rs service + consent.ts IPC binding + PetCanvas 加 `draggable` prop 解耦（onboarding webview 不要拖动）。**关键决策（9 条）**：① **节奏自适应** — 原拍板每段 1.2s → 改 `600 + 60×字`（5 段 ~10.5s），讨论时同步拍板；② **窗口形态独立** — 原 body 未拍板 → 独立 Tauri 窗 480×720（复用 multi-page Vite 范式）；③ **dev mode 入口** — 浏览器直访 `http://localhost:1420/onboarding.html`，IPC 失败 try/catch 不阻断（不依赖 Tauri runtime 也能视觉调试）；④ **默认 focus="再看一眼条款"** — 最慎重选项，防误回车 grant；⑤ **关闭语义统一退出** — "退出"/Alt+F4/X 都走 `getCurrentWindow().close()` → Rust `app.exit(0)`（与 pet/settings/chat 的 hide 反向，因 onboarding 失败 = 整个 app 终止）；⑥ **切窗 IPC `onboarding_complete`** — 后端统一控窗避免前端跨窗口编排；⑦ **method/version 双重锁死** — `consent_grant` IPC 同时校验 `method=='soul_pledge'` 与 `version==后端常量`，防 dev console 绕过 ADR-008 + 防前端硬编码 stale version；⑧ **bundle.resources 占位** — assets 通过 Vite `?raw` 构建期嵌入 chunk，运行时不读 resources，但 tauri.conf 保留配置作 #17 fs.readResource 占位；⑨ **reconsent UI 不做** — v2 data policy 上线时（M3）才扩展按钮文案"我重新确认"，本期 `check_version` 已返 `NeedReconsent` 路由通即可。**P0/P1 bug 检测 + 修复（主体完成后系统审查 5 个）**：① **ESC 关弹窗顺带退出** — window listener 在 bubble 阶段，EP ElDialog 的 body listener 先改 `showPolicy=false` 致守护失效。修：改 `document` + `capture:true`，让本 listener 先于 EP 看到事件；② **aria-live 形同虚设** — 所有段落预渲染只切 opacity，屏幕阅读器无感。修：每段 `<p>` 加 `:aria-hidden="i >= visibleCount"`；③ **onAgree 部分失败 toast 误导** — grant 成功 + complete 失败时 toast 说"同意写入失败"，但 DB 已写。修：分两段 try，区分语义（grant idempotent，complete 失败允许重试）；④ **focus 重复飞回** — grant 失败 → agreeing 翻 false → buttonsEnabled 翻 true → focus 又跑一次。修：`unwatchFocus()` 首次触发后取消订阅；⑤ **disabled 按钮区点击不能 skip** — `.soul-pledge__actions @click.stop` 拦住冒泡。修：去掉 stop（skip 幂等无副作用）。**99f53f0 UI polish**：弹窗过宽 / 滚动位置 / cursor 误导。**e993bd6 capability fix**：onboarding 窗"退出"按钮静默无反应；根因 = Tauri 2 capability 严格 grant，新窗口必须显式 allow 否则 IPC 静默无效（与 #10 / #11 同款踩坑路径，lessons 已有条目）。**测试**：pnpm typecheck / lint --max-warnings=0 / cargo check / pnpm build 全过；vite multi-page build 产出 `dist/onboarding.html` + 独立 chunk（onboarding.css 3.5KB / onboarding.js 45.5KB）。**Follow-up**：① onboarding webview 占内存 — tauri.conf 静态注册导致 Match 路径下 VRM 也被加载，#17 可改 `WebviewWindowBuilder` 动态创建或给 PetCanvas 加 v-if 守护；② `consent.check_version` 失败时绕过宣誓 — 当前 fail-safe（DB 坏时让用户能用 + eprintln），M3 数据治理加 FATAL 对话框是正路；③ #17 onboarding 状态机 — 监听 `onboarding:step-done` 接 Step 2 选人格 → ... → Step 6；④ v2 data policy reconsent UI — data_policy_v1.md §7 已规定路径，按钮文案改"我重新确认"。详见关闭的 [Issue #16](https://github.com/tl0502/APET/issues/16)。

### 立项准备期（2026-04-30 → 2026-05-05）

- 15 项 ADR 敲定（详见 [../../decisions.md](../../decisions.md)）
- 6 份基线文档归档（PRD / 架构 / flows / UAT / 人格 / 路线图）
- 立项档案：[../../research/competitor-research.md](../../research/competitor-research.md)
- 文档工程化重构 + 单人化简化（详见 [../../CHANGELOG.md](../../CHANGELOG.md) 同期条目）
- 项目记忆系统 + GitHub Issues 工作流设计（CLAUDE.md / STATUS.md / `/resumex` `/new-task` `/sync-status` / `.github/ISSUE_TEMPLATE/` / `.gitignore`）
- 远端仓库接入完成（https://github.com/tl0502/APET，6 milestones M1-M5 + P1，27 labels）

---

## 历史 session 摘要

### 2026-05-09 → 2026-05-11

完成 M1 W2 主体收官 4 issue（4 commit + 2 fix）：[#14](https://github.com/tl0502/APET/issues/14) ChatPanel V3 多对话并发版（commit a9f4b2c，2026-05-09→10）+ [#15](https://github.com/tl0502/APET/issues/15) 删 pet_nickname + 转场注入 announcement（commit 0bd425f，2026-05-10）+ [#20](https://github.com/tl0502/APET/issues/20) LLM Providers 多实例管理 + 模型探测（commit af07d90，2026-05-10）+ [#16](https://github.com/tl0502/APET/issues/16) 灵魂宣誓页（commit fdafeaa + 2 fix 99f53f0 / e993bd6，2026-05-11）。**M1 W2 主体可达**全部交付：用户可在独立 chat 窗多对话并发流式 + sidebar 新建/重命名/归档/删除 + 草稿持久化；昵称源唯一化为 .soul.md / 历史会话注入 system 转场行；6 preset Providers 多实例管理 + 模型探测下拉；新用户首启走灵魂宣誓 → consent 入库 → 切到主壳。**三类重大演化收纳**：① **#14 把 M3 B.3.d 多会话 CRUD UI 下沉到本 issue**（与 ChatGPT 式侧边栏基线认知差距过大）—— 后端 IPC 9 新增，前端 V3 多对话并发架构（ConvState reactive Map + Channel 闭包路由 + switchSeq race 守护 + 草稿双重 race 守护 + 启动期 orphan placeholder GC）；② **#15 完全删除 pet_nickname 整条线**（名字源唯一化为 .soul.md，避免双源），原 body 设计是保留 pet/user 双列；新增 announcement 整条线（原 body 没设计过昵称变更对历史对话的影响），基于 [arxiv 2402.10962](https://arxiv.org/html/2402.10962v1) Persona Drift 研究"history 中段 system 行直接重置话术比改 system prompt 更稳定"；③ **#16 onboarding 独立 Tauri 窗 480×720**（原 body 未拍板形态）+ 节奏自适应 `600 + 60×字`（原拍板每段 1.2s）+ "退出"统一走 `app.exit(0)`（与 pet/settings/chat 的 hide 反向，onboarding 失败 = 整个 app 终止）。**lessons §11 archived=0 三处守护扩展到 #14 #15**：ensure / set / update_last_activity 三处都守护 archived=0；昵称转场注入跨窗口归档时 active KV 可能短暂指向归档行，不守会注入幽灵 system 行 + 刷归档行时间戳。**Tauri 2 capability 严格 grant 第三类踩坑**：#16 fdafeaa 主体完成时 onboarding 窗"退出"按钮静默无反应；根因 = 新窗口必须显式加 capability 否则 IPC 静默无效（与 #10 / #11 同款路径）→ e993bd6 fix；提醒未来每开新窗口先列 capability 清单。**#16 P0/P1 bug 检测 + 修复（代码完成后系统审查 5 个）**：① ESC 关弹窗顺带退出（EP ElDialog body listener 先改 showPolicy 致守护失效 → 改 document + capture:true）；② aria-live 形同虚设（修：每段加 :aria-hidden）；③ onAgree 部分失败 toast 误导（修：分两段 try）；④ focus 重复飞回（修：首次触发取消订阅）；⑤ disabled 按钮区点击不能 skip（修：去掉 @click.stop）。**实测**：cargo test --lib **172 passed**（#13 121 + 新 51：#14 conversation CRUD/orphan placeholder GC/chat commands validation + #15 nickname_announcement 8 集成 + nickname 3 单测 + validate_nickname 4 单测 + #20 llm_providers partial update / 迁移老键 / 设 active）；4 issue 共触 typecheck / lint / cargo check / pnpm build 全过；6 preset 手测连通 + 模型列表拉取。**Follow-up 收纳**：M2 B.3.c 磁吸吸附 + pet 相对定位；M3 B.3.d 会话分组（今日/昨日/上周）+ 搜索 + 归档列表 UI + Markdown / 复制 / 重新生成 / 编辑；M3 G DPAPI 加密 API Key；M3+ 自动重试 + Tool calls + 非 OpenAI 协议；#17 onboarding 状态机 Step 2-6 + reconsent UI（v2 data policy 上线时）；onboarding webview VRM 加载内存占用（#17 改 `WebviewWindowBuilder` 动态创建或给 PetCanvas 加 v-if 守护）。下一步推 [#21](https://github.com/tl0502/APET/issues/21) M1 收尾（Onboarding Step 2-6 + LivingPet 自由活动初版 + VRM 微动作），完成后跨入 M2。

**2026-05-11（#16 后流程沉淀，无 commit）**：用户提元问题"issue body 需要这么详细吗？工作中发现新功能怎么办？`/resumex` 上下文恢复需要详细 body 吗？"。网络检索（[solo dev GitHub issues](https://kylehadland.com.au/blog/git-issues-solo) "capture speed > completeness"；[ccpm](https://github.com/automazeio/ccpm) 反方向 spec-driven 参考；[scope creep](https://www.hatica.io/blog/scope-creep-in-git/) "Phase 2 follow-up issue" 标准答案）+ 项目本地证据（[/resumex](../../../.claude/commands/resumex.md) 实际 `gh issue list` 字段不含 body；[github-workflow.md §9](../../github-workflow.md) 早已禁止"复制 PRD 内容到 body"；[lessons.md §2](../../lessons.md) 自承 4 次主动偏离 issue body 字面 schema）共同指向"详细 body 反成累赘"。**落地 3 项**：① [#16](https://github.com/tl0502/APET/issues/16) body 从 ~150 行压到 ~35 行（删 5 行宣誓 + 8 条 data_policy + Vue/Rust 伪代码全文复制；保留 6 段结构：目标 / 范围 / 拍板 / 不做 / 验收 / Refs）；② [.claude/commands/new-task.md](../../../.claude/commands/new-task.md) 示例 body 升级为 6 段结构模板 + 防错清单加 2 条（`body ≤ 50 行` / `不写组件伪代码 IPC 签名`）+ 「不要做」清单加 1 条；③ [docs/github-workflow.md §9](../../github-workflow.md) 反模式扩展 3 条（`不复制 PRD/ADR 全文`细化为"不写实现层伪代码"和`body > 50 行`两子条；提醒 `/resumex` 不读 body 全文）。**Why 值得记**：未来 issue 写作硬约束已落到 commands + 流程文档双层；证明"单人项目工具规范应来自项目自身证据而非外部教条"（4 条决定证据全来自项目内文件，业界检索仅做交叉验证）。**Follow-up**：[STATUS.md](../../STATUS.md) "当前 session 在做"字段已过时（仍说"#13 修正落地、#14/#15/#16 余下 bug 待下次 session"），下一 session 入场跑 `/sync-status` 同步到"M1 W1-W2 17/18 完成、剩 #21 M1 收尾"。

### 2026-05-07

完成 [Issue #13](https://github.com/tl0502/APET/issues/13) ChatService MVP + ADR-018 系统 prompt 拼装真业务对话上线（M1-D11，2 commit：fmt cleanup b6a8926 + ChatService 主体）：M1 W2 B 模块"真业务对话"上线，#14 ChatPanel 解锁。**为什么 #13 关键**：这是 ADR-018 三层抽象首次跑通完整链路（Persona+Nickname+Memory+Conversation+LLMProvider 5 service 编排），#14 ChatPanel 拿到 IPC 即用，无需再设计 prompt 拼装。**关键技术决策落地**：① **不切区段全塞 vs 切 4 节** — 选 **切 4 节**（# 身份/# 性格/# 能力/# 行为规则）；# 离线模板/# 反应配置 是本地代码池/物理交互 yaml，塞 LLM 是噪音；切区段函数 extract_persona_sections 按 H1 标题前缀匹配，14 个测试覆盖各种 case（"# 身份" / "# 身份(Identity)" / "# 身份 / Identity" 都识别）。② **静态全文 vs inline re-anchor 反 drift** — 选 **inline re-anchor**（[arxiv 2402.10962](https://arxiv.org/html/2402.10962v1) 学术支持）；system message 末句 + 每条 user 包装 "（保持 {persona_name} 风格）" 双管齐下；DB 仍存原始 input（ChatPanel 显示原文）仅 LLM 调用前包装。③ **离线降级抽 # 共情 vs # 拒答** — 选 **# 拒答**（issue body 字面 # 共情，但 # 共情 是用户表达情绪时用，"网络断了"抽 # 共情会违和；# 拒答 池语义最贴）；仅 LLMError::Network/ServerError 触发（AuthFailed/BadRequest 不抽，掩盖配置错误反害用户）。④ **claw-code 源码研究**（用户主动提供本地 D:/Project/temp/claw-code 仓库）确认：claw-code 是 Claude Code 的 Rust rewrite（不是 OpenClaw 的 SOUL.md 体系，网络搜索把两个项目混了），其 prompt.rs 用静态/动态 boundary sentinel + 截断守护（每文件 4000 chars / 总 12000）+ 祖先目录爬 CLAUDE.md 的设计；桌宠场景借鉴 boundary 概念（M1 函数拆分到位 / M3 接 prompt cache 加 sentinel 一行）+ 截断守护（per-section 4000 chars）；不抄爬祖先目录（桌宠没有"工作目录"概念）。**关键偏离 issue body**：① schema 没 user_state 表（D5 原则）→ active_conversation_id 走 config KV；② SendResult 加 conversationId 字段（issue body 仅 messageId，dev 验收/ChatPanel 切会话省一次 IPC）；③ load_active_persona 新增（原 PersonaService 只有 load_persona(id)，ChatService None 时需自动拿 active）。**关键学习收纳**：Anthropic AUP 触发 — 上次 session 中途因生成代码时涉及 ADR-006 安全前缀完整文案/泄露 API key/离线模板大段渲染被过滤器命中（"API Error: violates AUP"）；本次以 4 条规则恢复后顺利：① 安全前缀严守 M1 占位（一行 `let safety_prefix = ""; // TODO ADR-006 M3 G`）；② API key 全程占位 `sk-...`；③ 离线模板抽样路径处理（运行时 extract_refusal_templates）而非生成代码时贴大段；④ 每条响应只动 1 个文件。**用户产出 system prompt 管理指南** 23 KB / 593 行落盘 `D:\Project\temp\记录\ai桌宠-system-prompt-管理指南.md`，覆盖整体架构 + 5 常见 + 4 进阶微调场景 + 3 级验证 + 5 陷阱 + 版本管理 + 代码地标表。**测试**：cargo test --lib 96 passed（原 63 + 新 33）+ clippy chat/* 0 新 warning + pnpm typecheck 0 错 + cargo fmt 全仓库对齐 11 个 #5/#12 历史文件作单独 cleanup commit。**给 #14 提示**：3 个 IPC + 3 个事件已就位（chat_send / chat_cancel / chat_history + chat:stream:delta/done/error）；前端 binding 完整（src/services/chat.ts）；ChatPanel 设计 380×480 独立 chat 窗口 + ElInput multiline + 流式渲染 + cancel 按钮 + error 态分支；conversationId 从 sendChat 返回值拿，无需新 IPC。下一步推 [#14](https://github.com/tl0502/APET/issues/14) ChatPanel 形态 2 极简。

完成 [Issue #12](https://github.com/tl0502/APET/issues/12) LLMProvider OpenAI MVP + ADR-018 LLM 三层抽象框架（M1-D10，4 commit + 1 fix + 1 CUSTOM 扩展 + 1 STATUS 同步）：M1 W2 B 模块基础链上游交付。**ADR-018 入库**（commit 02fe910）：三层职责锁定 — Layer 1 LLMProvider（消息进 token 出 + tool_call 透传，#12 落地）/ Layer 2 ChatService（编排 Persona+Memory+SecurityGuard，#13 起完整）/ Layer 3 AgentService + ToolRegistry（Claude Code 风格 agent loop + Read/Edit/Write/Glob/Grep/Bash 6 工具，M3+ 新增；沙盒细则推 ADR-019）；同时覆盖架构 §6.1（callback 取代 impl Stream + Vec<ContentPart> 取代 String content；架构原文已加 superseded 注跳转）。**为什么扩展超出 issue body 字面**：用户明确未来要做多模态 + Claude Code 级本地文件能力；issue body 字面 `content: String` + 占位 ToolCall 设计 M3 接多模态/工具调用必重写 trait 拖累 ChatService 等所有 caller，故 M1 类型层一次到位（多写 ~50 行 typed-only enum/struct，省 M3 trait 重构成本）。**LLMProvider trait + OpenAIProvider 文本流式**（commit fddbe5e）：types 用 `Vec<ContentPart>` parts 数组（Text/ImageUrl/ImageBase64/Audio/File 5 variant）+ ToolCall/ToolDefinition typed only（M1 实际只走 Text + tools=vec![]，M3+ 接多模态/工具调用不动 trait）；OpenAIProvider 流式 + 取消（reqwest bytes_stream → eventsource_stream → on_delta callback；cancel.cancelled() 触发立即 break + drop stream 自动断 TCP）；error 7 分类（Network / AuthFailed / RateLimit / BadRequest / ServerError / Cancelled / ParseError）；序列化策略：单 Text part 序列化成旧 string `{role,content:"s"}` 兼容老 model 与 Ollama/Qwen/Moonshot fallback，多 part 走 parts 数组；process_event_stream 抽出来用合成 Bytes 流单测无需 mock HTTP server。**LLM IPC + 前端 binding**（commit c3a0f85）：4 个 IPC + ActiveTestRegistry Mutex<Option<CancellationToken>> 跨 IPC 共享；chat_send_test emit `chat:test:delta` 让 dev console listen 验真流式。**CUSTOM provider 扩展**（commit 11dc2cd）：加 set_openai_config({api_key?, base_url?, model?}) + get_openai_config() 让 dev console 用任意 OpenAI 兼容端点（DeepSeek/Moonshot/Qwen/Ollama）验证；架构 §0.1 6 preset 含 custom，但 issue body 字面只允许 set_openai_api_key 一个写入点，扩展是 dev 验证可行性的最小代价。**关键 fix tokio macros feature**（commit a93bf0d）：cargo feature 统一陷阱 — dev-deps tokio 启了 macros 让 cargo test 能跑，但非 test build（`pnpm tauri:dev`）不继承 dev-deps features，撞 `cannot find select in tokio`；补 `macros` feature 到主 deps；**M1 W2 起 cargo check 必须额外跑一遍 lib-only 路径**（不带 `--tests`）才算真过。**真实 dev 验证**（用户 DeepSeek 真 key）：① 黄金路径中文回复 + emit chat:test:delta 流式 ✅；② cancel 中途取消 → `Cancelled: cancelled by caller` ✅；③ AuthFailed 长 fake key 触发 DeepSeek 401 → `AuthFailed: 401 ...` ✅；④ Network 假 host 触发 ✅；⑤ cargo test 63 passed（原 46 + 新 17：types 5 + error 5 + openai 7）。**Provider 行为 quirk 收纳**：短 sk-WRONG 在 HTTP/2 层被 DeepSeek RST 落 Network 兜底；长 fake key 通过格式校验进入 status code 路径才能拿 401；OpenAI 在国内 GFW 拦 TLS 也只能拿 Network。结论：classify_status 5 分支单测已证代码正确，real-world 401 验证依赖 provider 协议与网络环境，不是代码问题。**关键偏离**：① schema 不保留 issue body 字面"settings 表 openai_api_key 字段"（#10 #11 同款守 27 表零迁移 D5 原则），改用 `config` 表 KV（key=`llm:openai:{api_key,base_url,model}`，M3 G CryptoService 上线后 api_key 迁 secrets+DPAPI）；② issue body 字面只允许 `set_openai_api_key` 一个写入点 → 加 `set_openai_config` 三键 partial update 让 CUSTOM provider 落地。下一步推 [#13](https://github.com/tl0502/APET/issues/13) ChatService MVP（Persona + Memory + Nickname → prompt 拼装 → 调 LLMProvider；阻塞 #14）。

完成 M1-D9 三 issue（同日 4 commit + 2 fix-up）：[#9](https://github.com/tl0502/APET/issues/9) 设置面板独立窗口骨架（commit 44bafbf）+ [#10](https://github.com/tl0502/APET/issues/10) 桌宠拖动 + 多屏位置持久化（commit 9d6c7e9 + fix 2a34cf7 capability）+ [#11](https://github.com/tl0502/APET/issues/11) 全局快捷键 Ctrl+Alt+Space + 冲突探测（commit fd227a2 + fix ff318b9 on_shortcut）。**M1 W2 主态可达**全部交付：用户可拖宠物到任意屏幕 / 关闭重启位置记忆；按 Ctrl+Alt+Space 弹 chat stub toast（焦点在 VSCode 时也能弹，跨窗口验全局）；托盘"设置"打开独立 800×600 窗口逐 tab 切换主题/Provider/人格/昵称/关于。**两类关键偏离收纳**：① schema 不保留 issue body 字面 `pet_runtime_state.last_position` / `settings.shortcut_chat` 字段（"27 表零迁移" D5 原则不破例），改用现有 `config` 表 KV（注释明确"运行时配置走此表"，语义零损失，与 active_conversation_id 同表）—— 适用 #10 #11；② Tauri 2 capability 严格 grant，`core:default` **不含** startDragging / set-position / monitor 系列，**必须**显式 allow 到 `capabilities/default.json` 否则静默无反应（无 console error），第一次 dev 视觉验证暴露此问题—— 这是 Tauri 2 默认更安全的设计，未来引入新 webview API 都要先看 `core:window` / `core:webview` 子项是否需补。**实测**：3 issue 共触 typecheck / lint / cargo check / cargo test 46 passed / pnpm build / pnpm tauri:build（release exe 28MB）全过；用户视觉验证拖动 + 跨窗口快捷键 + 主题切换全部确认。下一步推 [#12](https://github.com/tl0502/APET/issues/12) LLMProvider OpenAI MVP（B 模块基础链上游，阻塞 #13 #14）。

完成 [Issue #8](https://github.com/tl0502/APET/issues/8) 通用布局容器 AppShell + StandardDialog + useToast（commit 82ec827 + STATUS 同步 commit，将 push 触发自动关闭）：M1 W2 基础设施层。**新增 4 个文件 + 改 3 个文件**：`src/components/layouts/AppShell.vue`（~50L，两 variant：standalone 含 `[data-tauri-drag-region]` header + body + 可选 footer slot；transparent 纯语义包装无 chrome）；`src/components/feedback/StandardDialog.vue`（~65L，包 EP ElDialog，X / 遮罩 / ESC 三关闭路径透传，loading=true 屏蔽全部关闭路径 + body spinner overlay + footer 整体 `pointer-events:none + opacity:0.5`）；`src/composables/useToast.ts`（~60L，包 EP ElMessage，4 方法 success/error/info/warn=EP 'warning' + ToastAction 闭包绑 instance.close()）；`src/styles/components.css`（~140L，.aipet-shell / .aipet-dialog / .aipet-toast 样式覆写，全部 `--aipet-*` token，禁硬编码颜色 / 间距 / 字号）；`src/main.ts` CSS 加载顺序加 components.css（EP → dark → tokens → overrides → components → main）；`src/App.vue` 把 `.pet-shell` div 替换为 `<AppShell variant="transparent">` 真实生产消费，移除局部 style；`src/views/_dev/TokensPreview.vue` 追加 Components section（4 toast 按钮 + open dialog + toggle loading dialog 演示），同时修 preview 可滚动（`min-height: 100vh` → `height: 100vh; overflow-y: auto`，因 main.css 给 html/body/#app 设了 `overflow: hidden` 适配 pet 透明窗）。**关键取舍**：① 拖动用 Tauri 2 推荐的 `[data-tauri-drag-region]` attr 而非 issue body 字面 `-webkit-app-region: drag`（后者仅 macOS 原生支持，Windows WebView2 不识别）；② AppShell 不调 `useThemeStore().init()`（main.ts 全局已调，避免重复 init，`<html class=dark>` 全局生效）；③ StandardDialog 不内置 cancel/confirm（footer slot 自由，PRD §6 弹窗语义多样）；④ Toast 按钮 issue 字面 3 个 → 实际加 4 个覆盖完整 4 方法验收。**实测**：`pnpm typecheck` / `pnpm lint --max-warnings=0` / `pnpm build`（Vite v7.3.2，1622 modules，10.82s，dist 363.59 KB css + 1.71 MB js gzip 509 KB）三件套 0 错 0 警；`pnpm tauri:dev` 后台跑 setup 阶段 `[setup] reached` 后无 panic（plugin-sql preload 修复仍有效），HMR 动态推送 TokensPreview 样式变更生效；用户视觉验证 pet 窗口 320×320 透明 + AppShell variant=transparent 包装无副作用 + `?view=tokens` 4 toast 颜色/3000ms 关闭/Action 按钮 + dialog 三关闭路径 + loading toggle + 三态主题切换（auto/light/dark）全过。

完成 [Issue #7](https://github.com/tl0502/APET/issues/7) 视觉 token 体系 + 主题完整化（feat commit + STATUS 同步 commit + fix commit，将 push 触发自动关闭）：M1 W2 基础设施层。**新增 4 个文件 + 改 2 个文件**：`src/styles/tokens.css`（~40 项 `--aipet-*`，light 默认 + `:root.dark` 覆写，含 color/spacing/font/radius/shadow/motion/z-index 7 组）；`src/styles/element-overrides.css`（用项目 token 覆写 `--el-color-primary` / `--el-bg-color-*` / `--el-text-color-*` / `--el-border-color*` / `--el-border-radius-*` / `--el-font-*` / `--el-box-shadow*` / `--el-transition-duration*`，确保 EP 控件与自写组件视觉一致）；`src/composables/useTokens.ts`（`getToken(name)` 走 `getComputedStyle`，`getAipetToken(name)` 自动补 `--aipet-` 前缀）；`src/views/_dev/TokensPreview.vue`（色块 / spacing ruler / 字号 / 圆角 / 阴影 / motion 视觉对照 + 三态切换按钮）；`src/main.ts` 调整 CSS 加载顺序（EP → dark css-vars → tokens → EP 覆写 → main.css）+ dev-only `await import('@/views/_dev/TokensPreview.vue')` 当 `?view=tokens` 时挂 preview 而非 App（production build 不带 preview chunk）；`src/components/PetCanvas.vue` 把 `.hint/.hint-error/code` 的 6 处硬编码（颜色/字号/间距/圆角/字体/阴影）替换为 token，作为第一处消费示例。**关键取舍**：① 不引 router（项目目前没接，单页角色窗用不上）— preview 走 `?view=tokens` 查询参数 + dev-only 动态 import；② 不动 `useThemeStore` 主题驱动机制（保留 `<html class="dark">` toggle）— token 切换是纯 CSS variable swap 无 JS 重渲染；③ 透明窗口约束依旧由 `main.css` 兜底（`html/body/#app` 强制 `background:transparent`），`--aipet-color-bg` 仅供面板/preview 消费不灌到 body；④ 不做 Figma 同步 / 多品牌主题 / 字体打包 / 高对比度，全部按 issue body out-of-scope 推后。**实测**：`pnpm typecheck` / `pnpm lint --max-warnings=0` / `pnpm tauri:build` 三件套 0 错 0 警；`pnpm tauri:dev` 后台跑确认 setup 阶段 `[setup] reached` 后无 panic；视觉验证（用户跑 `pnpm tauri:dev`）：默认透明角色窗 PetCanvas loading/error 文案样式正常；`?view=tokens` preview 页色块/字号/圆角/阴影渲染正确，三态切换按钮即时切 light/dark。**顺手修 plugin-sql migrations 时序**（独立 commit）：`src-tauri/tauri.conf.json` 加 `plugins.sql.preload=['sqlite:aipet.db']`。**根因**：tauri-plugin-sql 2.4 的 migrations 默认 lazy（前端 `Database.load` 才 connect+migrate），而 lib.rs setup 期 `block_on(seed_builtin)` 用 `services/db.rs::open_app_db`（`create_if_missing(false)`）同步打开 db，时序上 db 文件还不存在 → SQLITE_CANTOPEN(14) → eprintln 失败（不阻塞启动但污染 stderr）。**修法**：plugin builder 注册 preload 列表后，plugin 自身 setup 会调 `DbPool::connect + Migrator::new + pool.migrate`（`tauri-plugin-sql/src/lib.rs:148-163`），且 plugin setup 在 builder.setup 之前执行。**实测**：再跑 `pnpm tauri:dev` 后台，`[setup] reached` 后 stderr 不再出现 `seed_builtin failed: ... unable to open database file`。**回归来源**：D5 完成时是 spawn fire-and-forget，前端先调过 `Database.load` 建库后 spawn 才到 open_app_db；2026-05-06 code-review #4 把 spawn 改 block_on 反转顺序，引入此回归（STATUS lib.rs:49 注释里"冷启 50-200ms 代价低于人感知阈值"换来了正确性，但漏了 plugin lazy migrations 的前提）。

完成 [Issue #6](https://github.com/tl0502/APET/issues/6) 系统托盘菜单（最小集 + 关闭语义，commit 558dafb，将 push 触发自动关闭）：M1 W2 主态可达交付物。**整文件复用 + 裁剪**旧项目 D:\Project\ai桌宠 cut/window/backend/services/ 的 tray.rs（125→105L）+ window_actions.rs（35L 0 改）；裁剪掉旧版 PASSTHROUGH/TOPMOST 2 个 CheckMenuItem（前者依赖未实现 AppState 字段，后者 #6 out-of-scope 推 M2）。**关键决策**：① 「显示/隐藏」按 issue 字面 1 项动态文案：MenuItem clone 多份传 closure，在菜单点击 / hover Enter 时 set_text 刷新；② 「单击托盘 toggle」按用户中途决策删除（仅菜单可操作避免误触）；③ tauri.conf.json `decorations:false` 故实际无 X 按钮，关闭路径只走 Alt+F4。**实测**：cargo check / pnpm typecheck / pnpm lint --max-warnings=0 三件套 0 错 0 警；视觉验证（用户跑 `pnpm tauri:dev`）：托盘图标 + tooltip + 右键 4 项菜单 + Alt+F4 hide + 退出杀进程全部通过。**memory 收纳**：feedback_file_ops（项目根全树读写已永久授权 + 内置 Edit/Write+相对路径 实测稳定，mcp filesystem 在 sandbox 下报 access denied 不用）+ feedback_validate_before_persist（用户提议先实测再写持久化产物，不可行就诚实反馈）。

### 2026-05-06

完成 [Issue #1](https://github.com/tl0502/APET/issues/1) M1-D1 项目脚手架：17 个文件落盘（5 前端配置 + 4 前端入口 + 5 Tauri 后端 + 1 capabilities + 2 icons），`pnpm install / typecheck / lint` + `cargo check` + `pnpm tauri:dev` 全部通过。**实施期发现**：本机 Windows HyperV TCP 排除范围 1423-1522 包含原计划端口 1430，改用 Tauri 2 默认 1420 + HMR 1421。**ADR-016**（脚手架技术栈）+ **ADR-017**（Element Plus 全量 import + 主题跟随系统）入库（commit 3426184）；脚手架代码 commit 8952e6e 已 push。

完成 [Issue #2](https://github.com/tl0502/APET/issues/2) Element Plus + 主题跟随系统（commit 7c387db）：EP 2.13 全量 import + zh-CN locale + dark css-vars；新建 `src/stores/theme.ts` 三态 Pinia store（auto/light/dark）+ matchMedia listener + localStorage 持久化；`useThemeStore().init()` 在 mount 之前调用避免 FOUC。**关键取舍**：不引 VueUse `useDark`（三态 mode 与其布尔语义不匹配）；验证 demo 不放 PetCanvas 主壳（违反 PRD §7.2 角色窗透明约束），改为功能性验证后即清理。**实测**：`pnpm tauri:build` release exe = **4.15 MB**（vs ADR-017 预估 ~9MB，偏好 53%），实测数字已写入 ADR-017。**同时清理 4 处文档过时性能预算**（`启动 < 1500ms / 内存 < 150MB` → 推到 M5 自测期统一压测）：decisions.md ADR-002、prd.md §22、architecture §M0 节点、roadmap §4.1 spike 表。

新建 [Issue #3](https://github.com/tl0502/APET/issues/3) 接入 PetCanvas + VRM momo 渲染（type:spike, module:A-shell, priority:p0），M1 W1 关键路径，下一 session 启动。

完成 [Issue #3](https://github.com/tl0502/APET/issues/3) PetCanvas + VRM momo 渲染（commit 0df9076，已 push 触发自动关闭）：装 three@0.184 + @pixiv/three-vrm@3.5 + @types/three(dev)；从旧项目 D:\Project\ai桌宠 整文件复制 src/services/vrm.ts（207L）+ src/composables/useVRMModel.ts（45L）；新写简化版 src/components/PetCanvas.vue（71L，剥离 hitbox/drag/IPC/petStore，仅保留 canvas + Loading/Error 兜底文案）；App.vue 引用 PetCanvas。**关键取舍**：vrm.ts 整文件复制（含未消费的 getBounds() ~50L），下个 hitbox task 直接复用避免重写；PetCanvas 严守 spike 边界（hitbox 推到 A.6，drag 推到 N 模块）；WebView 同源访问 public/avatar/avatar.vrm 无需额外 capabilities。**验收**：tauri:dev 视觉确认 momo 渲染正常 + 背景透明（半身像 / 双臂自然下垂 / 轻微呼吸）；typecheck / lint / cargo check 全过。

完成 [Issue #4](https://github.com/tl0502/APET/issues/4) IPC 框架（commit ff32dda，已 push 触发自动关闭）：与 #3 完全并行的后端壳；新建 `src-tauri/src/commands/{mod,system}.rs`（`#[tauri::command] async fn ping() -> "pong"`）+ `src/services/ipc.ts`（统一 `invoke<T>` wrapper，转发 `@tauri-apps/api/core::invoke` 并把异常包成 `IpcError` 带命令名上下文）+ `src/types/ipc.ts`（`IpcError` class）；`lib.rs` 注册 `invoke_handler![commands::system::ping]`。**关键取舍**：M1 不引 ts-rs/specta（手写类型；M2-M3 视规模决定）；alert 验证 demo 挂在 main.ts dev-only 守卫块（`if (import.meta.env.DEV)`），避开 PetCanvas 透明窗（PRD §7.2），验证通过后清理回原状态；`alert` 而非 `console.log` 是为了让单人 vibecoding 阶段的功能性验证最直观。**实测**：`pnpm tauri:dev` 弹出 `[ipc-verify] ping → pong`；`pnpm typecheck` / `pnpm lint --max-warnings=0` / `cargo check` 三件套 0 错 0 警。

完成 [Issue #5](https://github.com/tl0502/APET/issues/5) PersonaService MVP + Memory/Nickname 骨架（commit e5ca882，已 push 触发自动关闭）：M1 W1 数据层一次性入库。**整文件复用**旧项目 D:\Project\ai桌宠 dogfood 过的 4 个 .rs（persona/memory/nickname/test_db）+ 2 个 SQL migration（001_init 27 张表 + 002 persona_snapshots unique idx）+ momo.soul.md（单文件 + frontmatter）；后端 commands 层新写 3 个 wrapper（persona/nickname/memory）+ 前端 6 个 IPC binding（types + services）。**关键取舍**：① schema 走 architecture §4 完整版（27 表一次建，M2-M5 零迁移成本）而非 issue body §2 极简版（避免后续每个 milestone 写迁移）；② persona 资源用单文件 .soul.md 而非 issue body §1 的目录+meta.json（与 persona-design.md §4.1 / 旧项目 dogfood 一致）；③ persona.activate 写 DB（personas.is_active=1 + persona_snapshots UPSERT），跨重启保留，不走 issue body 字面"暂存内存"；④ services/memory.rs 含 messages 表 CRUD 超集，#5 内未消费，加 mod 级 `#[allow(dead_code)]` 屏蔽 warning，M1 W2 ChatService MVP 接入时去掉。**依赖**：tauri-plugin-sql 2.x（建库 + WAL）+ sqlx 0.8（service 层短期连接，plugin DbPool 在 2.x 不公开）+ gray_matter 0.2 yaml-only + ulid 1。**实测**：`cargo test` 38 测试全过（DB 集成 + 单元）；`cargo check` / `pnpm typecheck` / `pnpm lint --max-warnings=0` 三件套 0 错 0 警；`pnpm tauri:dev` setup 阶段 `[setup] reached` 后无 panic、`seed_builtin` 异步 UPSERT 内置 momo 无 eprintln 失败。

### 2026-05-05

1. **文档工程化重构**：文件夹改名（中文 → 英文）、统一 YAML frontmatter（3 字段）、删悬空引用、新增 6 份工程标准件。详见 [../../CHANGELOG.md](../../CHANGELOG.md)。
2. **单人化简化**：删 M0 决策周设定、ADR 折叠到 `decisions.md` 单文件、删 KPI 阈值门禁、roadmap 删多层任务粒度。
3. **项目记忆系统**：建 `CLAUDE.md` / `STATUS.md` / `/resumex` 命令 / `templates/status-template.md`；WORKFLOW.md 加 §8。
4. **GitHub Issues 工作流设计**：建 `docs/github-workflow.md`（labels / milestones / 命名约定 / 接入步骤）；建 `.github/ISSUE_TEMPLATE/{feat,spike,fix}.yml`；新增 `/new-task` 与 `/sync-status` 命令；升级 `/resumex` 让它读最近 5 个开放 issue；新增 `.gitignore`。
5. **GitHub 仓库接入**：commit `13ab823` / `4afbae8` 已 push 到 https://github.com/tl0502/APET ；建 6 milestones + 27 labels；接入完成。
