{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "默认能力集 — pet 窗口可使用核心 IPC（事件 listen/emit、窗口操作等）。",
  "windows": ["pet"],
  "permissions": [
    "core:default"
  ]
}
